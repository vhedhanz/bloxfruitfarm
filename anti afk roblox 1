-- ===============================================
-- LOGIKA PEMBARUAN PING MENGGUNAKAN TICK (Metode Alternatif)
-- Ganti kode "Logika Pembaruan FPS & PING" dengan ini.
-- ===============================================

local RemoteEvent = game:GetService("ReplicatedStorage"):FindFirstChild("SomeRemoteEvent") -- Coba cari RemoteEvent yang sudah ada
if not RemoteEvent then
    -- Jika tidak ada RemoteEvent, buat satu di ReplicatedStorage (hanya jika Anda bisa)
    -- Namun, membuat objek baru di game lain mungkin tidak diizinkan oleh executor Anda.
    -- Jadi, jika tidak ada, metode ini mungkin juga gagal.
end


if RemoteEvent then
    RemoteEvent.OnClientEvent:Connect(function(startTime)
        local ping = math.floor((tick() - startTime) * 1000 / 2)
        PingText.Text = "PING: " .. ping .. " ms (Approx)"
    end)

    while true do
        RemoteEvent:FireServer(tick())
        wait(1)
    end
else
    PingText.Text = "PING: Error (No Remote)"
end
